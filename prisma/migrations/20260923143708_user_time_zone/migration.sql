-- Часовий пояс пристрою користувача (IANA, напр. "Europe/Kyiv").
-- Потрібен серверу там, де браузера поруч немає: генерація повторюваних
-- завдань і підрахунок серії. "UTC" — безпечне значення до першого
-- відкриття застосунку, далі браузер надсилає справжню зону.

-- AlterTable
ALTER TABLE "User" ADD COLUMN     "timeZone" TEXT NOT NULL DEFAULT 'UTC';
